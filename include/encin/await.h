#pragma once

#include <assert.h>
#include <stdbool.h>

#define encin_await(task)                                                                          \
    ({ static_assert(false, "encin_await is not implemented yet"); })
