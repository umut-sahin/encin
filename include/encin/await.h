#pragma once

#include <assert.h>
#include <stdbool.h>

#define encin_await(task)                                                                          \
    static_assert(false, "encin_await is not implemented yet");

#define encin_awaitv(task)                                                                         \
    static_assert(false, "encin_awaitv is not implemented yet");
